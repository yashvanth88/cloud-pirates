# Secrets template (placeholders only)
# - Do NOT commit real secret values.
# - Use this file as a local development template or reference for GitHub Actions secret names.

# Civo
CIVO_API_KEY=__CIVO_API_KEY__
CIVO_CLUSTER_NAME=__CIVO_CLUSTER_NAME__

# Database
# Example: postgres://username:password@host:5432/dbname
DATABASE_URL=postgres://dbuser:securepass@db.example.com:5432/pirates_db

# Object storage (S3-compatible / MinIO)
SPACES_KEY=__SPACES_KEY__
SPACES_SECRET=__SPACES_SECRET__
SPACES_ENDPOINT=https://minio.default.svc.cluster.local:9000
SPACES_BUCKET=pirates-scans

# Redis (queue)
REDIS_URL=redis://127.0.0.1:6379

# Docker registry credentials (used by CI to push images)
# If using GitHub Container Registry (GHCR): DOCKER_REGISTRY_HOST=ghcr.io and DOCKER_REGISTRY_USERNAME=<your-gh-user>
DOCKER_REGISTRY_HOST=ghcr.io
DOCKER_REGISTRY_USERNAME=your_registry_username
DOCKER_REGISTRY_PASSWORD=your_registry_token
REGISTRY_OWNER=your-org-or-username

# Inference provider (optional)
INFERENCE_URL=https://api.example-inference.com/v1/predict
INFERENCE_API_KEY=__INFERENCE_API_KEY__

# Observability / Errors
SENTRY_DSN=

# Usage:
# - For local development: copy this to `.env` and edit values for your local/dev services.
#   Example: `cp secrets-template.env .env` then edit `.env` (do not commit `.env`).
# - For CI/CD and production: add the corresponding values to GitHub repository Secrets (Settings → Secrets and variables → Actions).
#   Required secrets for the provided workflow:
#     - CIVO_API_KEY
#     - CIVO_CLUSTER_NAME
#     - DOCKER_REGISTRY_USERNAME
#     - DOCKER_REGISTRY_PASSWORD
#     - DOCKER_REGISTRY_HOST (optional, defaults to ghcr.io)
#     - REGISTRY_OWNER (optional, defaults to repository owner)
#     - SPACES_KEY
#     - SPACES_SECRET
#     - DATABASE_URL
#     - REDIS_URL
#     - INFERENCE_API_KEY (if using an external inference provider)
